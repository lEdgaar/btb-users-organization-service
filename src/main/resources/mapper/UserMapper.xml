<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tu.paquete.mapper.UserMapper">

    <!-- Obtener un usuario por ID -->
    <select id="getUserById" resultType="com.btb.usersorganizationservice.entity.User">
        SELECT * FROM users WHERE id = #{id}
    </select>

    <!-- Insertar un nuevo usuario -->
    <insert id="insertUser" parameterType="com.tu.paquete.model.User">
        INSERT INTO users (email, created_at, date_of_birth, gender, password, last_password_date,
                           role_type_id, organization_id, is_deleted, date_deleted, is_blocked,
                           date_blocked, country_id)
        VALUES (#{email}, #{created_at}, #{date_of_birth}, #{gender}, #{password},
                #{last_password_date}, #{role_type_id}, #{organization_id}, #{is_deleted},
                #{date_deleted}, #{is_blocked}, #{date_blocked}, #{country_id})
    </insert>

    <!-- Actualizar información del usuario -->
    <update id="updateUser" parameterType="com.tu.paquete.model.User">
        UPDATE users
        SET email = #{email}, created_at = #{created_at}, date_of_birth = #{date_of_birth},
            gender = #{gender}, password = #{password}, last_password_date = #{last_password_date},
            role_type_id = #{role_type_id}, organization_id = #{organization_id},
            is_deleted = #{is_deleted}, date_deleted = #{date_deleted}, is_blocked = #{is_blocked},
            date_blocked = #{date_blocked}, country_id = #{country_id}
        WHERE id = #{id}
    </update>

    <!-- Eliminar un usuario por ID -->
    <delete id="deleteUserById">
        DELETE FROM users WHERE id = #{id}
    </delete>

</mapper>
